<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <title>Asjeno.pl - Czat</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #e6ffe6; /* zielone tło */
      color: #0044cc; /* niebieski tekst */
      display: flex;
      flex-direction: column;
      height: 100vh;
      margin: 0;
    }
    header {
      background: #28a745;
      color: white;
      text-align: center;
      padding: 15px;
      font-size: 20px;
    }
    #chat {
      flex: 1;
      overflow-y: auto;
      padding: 20px;
      background: #f9fff9;
      border-top: 2px solid #28a745;
      border-bottom: 2px solid #28a745;
    }
    .message {
      padding: 8px 12px;
      margin: 8px 0;
      border-radius: 15px;
      max-width: 70%;
      word-wrap: break-word;
    }
    .me {
      background: #28a745;
      color: white;
      margin-left: auto;
    }
    .other {
      background: #0044cc;
      color: white;
      margin-right: auto;
    }
    #form {
      display: flex;
      padding: 10px;
      background: white;
      border-top: 2px solid #28a745;
    }
    #input {
      flex: 1;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 10px;
    }
    #send {
      margin-left: 10px;
      padding: 10px 20px;
      background: #28a745;
      color: white;
      border: none;
      border-radius: 10px;
      cursor: pointer;
    }
    #send:hover {
      background: #218838;
    }
  </style>
</head>
<body>
  <header>💬 Asjeno.pl - Czat</header>

  <div id="chat"></div>

  <form id="form">
    <input id="input" type="text" placeholder="Napisz wiadomość..." required />
    <button id="send" type="submit">Wyślij</button>
  </form>

  <script>
    const chat = document.getElementById("chat");
    const form = document.getElementById("form");
    const input = document.getElementById("input");

    form.addEventListener("submit", function(e) {
      e.preventDefault();
      const text = input.value.trim();
      if (!text) return;

      // moja wiadomość
      addMessage(text, "me");

      // przykładowa odpowiedź bota
      setTimeout(() => {
        addMessage("Odpowiedź znajomego: " + text, "other");
      }, 1000);

      input.value = "";
    });

    function addMessage(text, type) {
      const div = document.createElement("div");
      div.classList.add("message", type);
      div.textContent = text;
      chat.appendChild(div);
      chat.scrollTop = chat.scrollHeight;
    }
  </script>
</body>
</html>
